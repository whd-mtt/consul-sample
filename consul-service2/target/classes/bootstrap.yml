spring:
  cloud:
    consul:
      enabled: true
      host: 127.0.0.1
      port: 8500
      discovery:
        register-health-check: true
        health-check-path: /actuator/health
server:
  port: 8881
  servlet:
    context-path: /service2

